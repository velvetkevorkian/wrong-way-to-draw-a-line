{
  "name": "wrong-way-to-draw-a-line",
  "version": "0.0.1",
  "dependencies": {
    "@velvetkevorkian/sketch-boilerplate-p5": "0.0.2"
  },
  "scripts": {
    "build": "webpack --config webpack.production.js",
    "start": "webpack-dev-server --config webpack.dev.js",
    "ship": "rsync -avz --delete wrong-way-to-draw-a-line $npm_config_labs_deploy_path",
    "deploy": "npm run build && npm run ship"
  },
  "license": "ISC"
}
